<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MyGoogleBooksProject" parent="Theme.MaterialComponents.DayNight">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primaryGreen</item>
        <item name="colorPrimaryVariant">@color/primaryGreen</item>
        <item name="colorOnPrimary">@color/primaryGreen</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/accentGreen</item>
        <item name="colorSecondaryVariant">@color/secondaryGreen</item>
        <item name="colorOnSecondary">@color/black</item>
        <item name="colorAccent">@color/accentGreen</item>
        <item name="colorButtonNormal">@color/buttonsGreen</item>
        <item name="colorBackgroundFloating">@color/secondaryGreen</item>
        <item name="actionButtonStyle">@color/secondaryGreen</item>
        <item name="android:windowLightStatusBar">true</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>

        <item name="floatingActionButtonSmallStyle">@color/accentGreen</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="buttonStyle">
        <item name="backgroundTint">@color/buttonsGreen</item>
        <item name="cornerRadius">21dp</item>
        <item name="textFillColor">@color/white</item>
    </style>

</resources>